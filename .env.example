# Public site origin (no trailing slash). Used in index.html for canonical and Open Graph URLs.
VITE_SITE_URL=http://localhost:5173

# API URL. Used in API requests.
VITE_API_URL=http://localhost:3000
